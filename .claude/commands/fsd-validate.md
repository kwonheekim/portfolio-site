# FSD Validate - FSD 규칙 검증

프로젝트의 FSD 패턴 준수 여부를 검증하고 위반 규칙을 찾습니다.

---

## 📋 검증 항목

이 명령어는 다음을 확인합니다:

### 1. 의존성 규칙 준수
```
✅ 가능 의존성
  app → 모든 곳
  pages → features, entities, widgets, shared
  features → entities, shared
  entities → shared
  widgets → shared
  shared → 아무것도 참조 불가

❌ 금지된 의존성
  shared → features (순환 참조)
  entities → features (위계 위반)
  pages → app (위계 위반)
```

### 2. 폴더 구조 검증
```
✅ 올바른 구조
  feature/section-name/
  ├── ui/              (컴포넌트)
  ├── model/           (상태 및 로직)
  ├── lib/             (유틸)
  ├── api/             (선택사항)
  └── index.ts         (public API)

❌ 위반 구조
  feature/section-name/
  ├── Hero.tsx         (ui/ 없음)
  ├── hooks.ts         (model/ 없음)
  └── utils.ts         (lib/ 없음)
```

### 3. Import 경로 검증
```
✅ 올바른 import
  import { Hero } from '@/features/hero'

❌ 잘못된 import
  import { Hero } from '@/features/hero/ui/Hero'
  import { Hero } from 'src/features/hero/ui/Hero'
```

### 4. Index.ts 검증
```
✅ public API 정의
  export { Hero } from './ui/Hero'
  export { useHeroData } from './model/hooks'
  export type { HeroProps } from './model/types'

❌ 누락
  (index.ts 파일 없음)
  (export 없음)
```

---

## 🚀 사용 방법

### 명령어 실행
```bash
/fsd-validate
```

또는 VS Code Command Palette에서:
- `Cmd+Shift+P` / `Ctrl+Shift+P`
- "fsd-validate" 검색
- Enter

---

## 📊 출력 형식

### 성공 결과
```
═══════════════════════════════════════════════════════════
FSD 검증 보고서
═══════════════════════════════════════════════════════════

전체 상태: ✅ PASS (모든 규칙 준수)

📊 검증 결과
────────────────────────────────────────────────────────
1. 의존성 규칙: ✅ PASS
   - 검사된 파일: 42개
   - 위반: 0개

2. 폴더 구조: ✅ PASS
   - Feature: 5개 (모두 정상)
   - Widget: 2개 (모두 정상)
   - Entity: 4개 (모두 정상)

3. Import 경로: ✅ PASS
   - 올바른 경로: 138개
   - 잘못된 경로: 0개

4. Index.ts: ✅ PASS
   - 정의됨: 11개
   - 누락: 0개

📈 통계
────────────────────────────────────────────────────────
총 파일: 157개
총 모듈: 11개
의존성 관계: 89개
평균 의존성 깊이: 2.3

✅ 모든 검증 통과!
```

### 경고가 있는 결과
```
═══════════════════════════════════════════════════════════
FSD 검증 보고서
═══════════════════════════════════════════════════════════

전체 상태: ⚠️ WARNING (경고 3개)

🔍 발견된 이슈
────────────────────────────────────────────────────────

⚠️ 의존성 규칙 위반 (3개)

1. src/features/hero/ui/Hero.tsx (라인 5)
   ❌ shared가 features를 참조

   import { useHeroData } from '@/features/hero/model/hooks'
   ─────────────────────────────────────────────────────────
   문제: shared 계층이 features를 참조할 수 없음

   🔧 수정 방법:
      - Hero 로직을 features/hero로 이동
      - 또는 shared/hooks로 추상화

   참고: /fsd-migrate 명령어로 상세 마이그레이션 가이드 확인

2. src/components/Header.tsx (라인 12)
   ❌ 잘못된 import 경로

   import { Hero } from '@/features/hero/ui/Hero'
   ─────────────────────────────────────────────────────────
   문제: index.ts를 거치지 않고 직접 import

   🔧 수정 방법:
      import { Hero } from '@/features/hero'
      ═══════════════════════════════════════

3. src/features/projects/
   ⚠️ index.ts 누락

   파일 구조:
   ├── ui/Projects.tsx
   ├── model/
   └─  └── (index.ts 없음!) ← 여기

   🔧 수정 방법:
      // src/features/projects/index.ts 생성
      export { Projects } from './ui/Projects'
      export { useProjects } from './model/hooks'

📈 요약
────────────────────────────────────────────────────────
✅ 통과: 8개
⚠️ 경고: 3개
❌ 실패: 0개

권장사항:
1. 우선순위: 의존성 규칙 위반 수정 (highest)
2. import 경로 정정 (medium)
3. index.ts 추가 (low)

💡 팁: /fsd-refactor 명령어로 자동 수정 가능
```

### 실패 결과
```
═══════════════════════════════════════════════════════════
FSD 검증 보고서
═══════════════════════════════════════════════════════════

전체 상태: ❌ FAIL (심각한 문제 2개)

🚨 심각한 문제 (자동 수정 불가)
────────────────────────────────────────────────────────

❌ 순환 참조 감지

A) src/features/hero → src/features/projects → src/features/hero

   경로:
   hero/model/hooks.ts
   → imports from projects/lib/utils.ts
   → imports from hero/ui/Hero.tsx

   문제: 순환 의존성 발생

   🔧 수정 방법:
      1. 순환 참조 파일 식별
      2. 공통 로직을 shared/로 추출
      3. import 구조 단방향으로 정리

B) src/entities/project ↔ src/features/hero

   entities가 features를 참조하면 안 됨!

   🔧 수정 방법:
      - entities/project는 순수한 타입 정의만
      - features에서 entities 참조 (역은 불가)

💡 도움말:
/fsd-refactor 실행 시도 (자동 수정)
/ fsd-migrate 마이그레이션 가이드 참고
```

---

## 🎯 검증 규칙 상세

### 규칙 1: 의존성 방향성
```javascript
// 허용 관계만 확인
const ALLOWED_DEPENDENCIES = {
  'app': ['processes', 'pages', 'features', 'entities', 'widgets', 'shared'],
  'processes': ['pages', 'features', 'entities', 'shared'],
  'pages': ['features', 'entities', 'widgets', 'shared'],
  'features': ['entities', 'shared'],
  'entities': ['shared'],
  'widgets': ['shared'],
  'shared': []  // 아무것도 참조 불가
}
```

### 규칙 2: 폴더 명명 규칙
```
✅ 올바른 이름
  src/features/hero-section/      (kebab-case)
  src/entities/project/            (kebab-case)
  src/widgets/header/              (kebab-case)

❌ 잘못된 이름
  src/features/HeroSection/        (PascalCase)
  src/features/hero_section/       (snake_case)
```

### 규칙 3: Public API (index.ts)
```
✅ 올바른 예
// src/features/hero/index.ts
export { Hero } from './ui/Hero'
export { useHeroData } from './model/hooks'
export type { HeroProps } from './model/types'

❌ 잘못된 예
// src/features/hero/index.ts
export * from './ui/Hero'      // 너무 넓음
// 또는 파일 없음
```

### 규칙 4: 내부 구조
```
✅ Segment 분류
  ui/       - 컴포넌트
  model/    - 상태, 훅, 타입
  lib/      - 유틸, 상수
  api/      - API 호출 (선택)

❌ 혼란스러운 구조
  components/
  hooks.ts
  store.ts
  utils.ts
```

---

## 🔧 검증 결과 활용

### 통과한 경우 ✅
```
축하합니다! FSD 패턴을 완벽하게 준수하고 있습니다.

다음 단계:
1. 프로젝트 규모 확대 시에도 이 구조 유지
2. 새 개발자에게 README.md 공유
3. /fsd-generate로 새 feature 추가
```

### 경고가 있는 경우 ⚠️
```
/fsd-refactor 명령어로 자동 수정 시도:
/fsd-refactor --auto-fix

또는 수동으로:
1. 각 이슈별로 파일 수정
2. npm run build로 빌드 확인
3. /fsd-validate로 재검증
```

### 실패한 경우 ❌
```
/fsd-migrate 마이그레이션 가이드 참고:
/fsd-migrate

또는:
1. 순환 참조 끊기 (공통 로직 shared로 추출)
2. 의존성 방향 정정
3. 폴더 구조 재정렬
4. /fsd-validate로 재검증
```

---

## 📋 검증 전체 플로우

```
/fsd-validate 실행
    ↓
1. 폴더 구조 검사
    ├─ layers 확인 (app, pages, features, ...)
    ├─ slices 확인 (folder structure)
    └─ segments 확인 (ui, model, lib, api)
    ↓
2. Import 분석
    ├─ 모든 import 추출
    ├─ 경로 정규화
    └─ 의존성 그래프 생성
    ↓
3. 규칙 검증
    ├─ 의존성 방향성 확인
    ├─ Index.ts 존재 확인
    └─ Naming convention 확인
    ↓
4. 결과 생성
    ├─ PASS/WARNING/FAIL 판정
    ├─ 이슈 나열
    └─ 수정 제안
```

---

## 💡 팁

### 정기적 검증
```bash
# 개발 중에도 계속 확인
/fsd-validate

# git pre-commit hook으로 자동 검증
# (선택사항)
```

### 대규모 프로젝트
```bash
# 특정 레이어만 검증
/fsd-validate --layer features

# 특정 모듈만 검증
/fsd-validate --module hero
```

### CI/CD 통합
```bash
# GitHub Actions에서
npm run build  # 빌드 확인
/fsd-validate  # FSD 검증
```

---

## 🎯 다음 단계

### 이슈가 없는 경우
```bash
✅ 프로젝트는 FSD 패턴 준수 중

다음:
/fsd-generate feature-name  # 새 feature 추가
```

### 이슈가 있는 경우
```bash
⚠️ 또는 ❌ 이슈 발견

다음:
/fsd-refactor               # 자동 수정 시도
/fsd-migrate                # 마이그레이션 가이드
```

---

**FSD 리소스**: https://feature-sliced.design/

🔍 Generated with Claude Code

# Project Start - Smart Conditional Execution with Error Checking

코드 수정 후 프로젝트를 지능적으로 실행합니다.
**package.json 변경 여부를 감지**하여 필요한 작업만 수행하고,
**lint-error-fixer 에이전트로 오류를 먼저 검증**한 후 실행합니다.

---

## 🚀 **스마트 실행 로직 (에이전트 검증 포함)**

### 전체 흐름
```
1️⃣ 빌드 → 2️⃣ 에이전트 검증 → 3️⃣ 오류 없음 → 4️⃣ 로컬 실행
                              ↓ 오류 발견
                              오류 보고 및 중단
```

---

## 🚀 **상세 실행 로직**

### 시나리오 1️⃣: 새 라이브러리가 설치되었을 때 ➕

```bash
# package.json에 새로운 의존성이 추가된 경우
npm install → npm run build → lint-error-fixer 검증 → ✓ 오류 없음 → npm run dev (로컬 실행)
                                                      ↓ 오류 발견
                                                      오류 보고 및 중단
```

**예시**:
- `npm install axios` 후 `/project-start` 실행
- 자동으로 설치, 빌드, 검증 진행
- 오류 없으면 개발 서버 실행

### 시나리오 2️⃣: package.json 변경 없을 때 ⚡

```bash
# 코드만 수정한 경우
npm run build → lint-error-fixer 검증 → ✓ 오류 없음 → npm run dev (로컬 실행)
                                        ↓ 오류 발견
                                        오류 보고 및 중단
```

**예시**:
- `src/features/hero/ui/Hero.tsx` 파일만 수정 후 `/project-start` 실행
- `npm install` 스킵 (빠른 실행)
- 빌드 → 검증 → 오류 없으면 서버 실행

### 시나리오 3️⃣: 빌드 성공했으나 오류 발견 시 ⚠️

```bash
# 빌드는 성공했지만 lint-error-fixer에서 문제 감지
npm run build (성공) → lint-error-fixer 검증 → ✗ 오류 발견 → 오류 보고
                                              npm run dev 실행 안 함 (중단)
```

**예시**:
- TypeScript 타입 에러 있음
- Import 경로 잘못됨
- 미사용 변수 있음
- 보안 취약점 감지

### 시나리오 4️⃣: 빌드 실패 시 🚨

```bash
# 빌드 실패 감지
npm run build (실패) → npm install 재시도 → npm run build (재시도)
                                          → lint-error-fixer 검증
                                          → 오류 보고 (로컬 실행 안 함)
```

**예시**:
- 빌드 에러 발생
- 자동으로 의존성 재설치 후 재빌드 시도
- 여전히 실패하면 오류 보고

---

## 🔍 **자동 감지 프로세스**

### Step 1: package.json 버전 확인
```
package.json (현재)과 package-lock.json (설치됨)을 비교
→ 차이 발생 = 새 라이브러리 설치 필요
```

### Step 2: node_modules 상태 확인
```
node_modules 폴더 존재 여부 확인
→ 없음 = npm install 필수
```

### Step 3: 빌드 캐시 확인
```
build/ 폴더 최신 상태 확인
→ 없거나 오래됨 = 재빌드 필요
```

### Step 4: 실행 (에이전트 분석)
```
프로젝트 상태 자동 분석
→ 에러 있으면 알림
→ 정상이면 개발 서버 시작
```

---

## 📊 **실행 플로우 (에이전트 검증 포함)**

```
┌─────────────────────────────────────┐
│      /project-start 명령어 실행      │
└──────────────┬──────────────────────┘
               │
        ┌──────▼──────┐
        │ package.json │
        │  변경 감지?  │
        └──┬────────┬──┘
           │        │
         YES        NO
           │        │
    ┌──────▼─┐  ┌───▼──────┐
    │npm     │  │ 스킵     │
    │install │  │          │
    └──────┬─┘  └───┬──────┘
           │        │
           └──┬──────┘
              │
        ┌─────▼──────┐
        │npm run build│
        │   성공?    │
        └──┬───────┬──┘
           │       │
         YES      NO
           │       │
    ┌──────▼─────────────┐ ┌──▼─────────┐
    │ lint-error-fixer   │ │npm install │
    │ (에이전트 검증)    │ │재시도      │
    └──┬───────────┬─────┘ └──┬─────────┘
       │           │          │
    ✓ OK       ✗ ERROR    ┌─────▼──────┐
       │           │       │npm run build│
       │    ┌──────▼────────┤   재시도    │
       │    │ 오류 보고서    └──┬───────┬──┘
       │    │ (실행 중단)        │       │
       │    │ - 에러 목록        │     YES (재시도)
       │    │ - 권장사항    ┌────▼───────▼──┐
       │    │ - 수정 방법   │lint-error-fixer│
       │    │              │  (최종 검증)   │
       │    │              └──┬───────┬──┘
       │    │                 │       │
       │    │              ✓ OK   ✗ ERROR
       │    │                 │       │
       │    └─────────────────┘       │
       │                              │
       │                         ┌────▼──────────┐
       │                         │최종 오류 보고서 │
       │                         │(실행 중단)     │
       │                         │- 모든 오류     │
       │                         │- 수정 필요     │
       │                         └────────────────┘
       │
       ┌─────▼────────────┐
       │npm run dev (로컬) │
       │   실행 시작        │
       └──────┬───────────┘
              │
       ┌──────▼──────────────┐
       │개발 서버 준비 완료   │
       │http://localhost:3000│
       └─────────────────────┘
```

---

## 🎯 **실행 흐름 상세**

### 방법 1️⃣: 명령어 실행 (권장)

```bash
/project-start
```

VS Code Command Palette에서:
- `Cmd+Shift+P` (Mac) 또는 `Ctrl+Shift+P` (Windows)
- "project-start" 검색
- Enter 클릭

### 방법 2️⃣: 수동 명령어 (참고용)

**새 라이브러리 설치 후:**
```bash
npm install new-package
npm install  # 의존성 확인
npm run build
npm run dev
```

**코드만 수정한 경우:**
```bash
npm run build
npm run dev
```

---

## 🧠 **스마트 감지 로직**

### 변경 감지 알고리즘

```javascript
// 감지 순서 (우선도)

1️⃣ package.json vs package-lock.json 비교
   → 버전 차이 감지 = npm install 필요

2️⃣ node_modules 폴더 확인
   → 없음 = npm install 필요
   → 불완전 = npm install 필요

3️⃣ build/ 폴더 상태 확인
   → 없음 = npm run build 필요
   → 수정 시간 > package.json 수정 시간 = 재빌드 필요

4️⃣ src/ 폴더 수정 시간 확인
   → src 수정 시간 > build 폴더 시간 = 재빌드 필요
```

### 각 단계별 체크

| 단계 | 체크 항목 | 결과 | 다음 단계 |
|------|---------|------|---------|
| **1** | package.json 변경 | YES | npm install |
| | | NO | 단계 2로 |
| **2** | node_modules 존재 | YES | 단계 3로 |
| | | NO | npm install |
| **3** | build/ 폴더 존재 | YES | 단계 4로 |
| | | NO | npm run build |
| **4** | 빌드 캐시 유효 | YES | npm run dev |
| | | NO | npm run build |
| **5** | 빌드 성공 | YES | npm run dev |
| | | NO | npm install + 재시도 |
| **6** | 개발 서버 시작 | SUCCESS | 에이전트 분석 |

---

## 💡 **사용 예시**

### 예시 1️⃣: 새 라이브러리 추가 (검증 통과)
```bash
# 1. 라이브러리 설치
npm install axios

# 2. /project-start 실행
/project-start

# 자동 실행:
# ✓ npm install (변경 감지)
# ✓ npm run build (성공)
# ✓ lint-error-fixer 검증 (오류 없음)
# ✓ npm run dev (로컬 실행)
# ✓ http://localhost:3000 접속 가능
```

### 예시 2️⃣: 코드 수정 (검증 실패)
```bash
# 1. 파일 수정 (예: src/features/hero/ui/Hero.tsx)
# ... 타입 에러 포함된 파일 편집 ...

# 2. /project-start 실행
/project-start

# 자동 실행:
# ✓ npm install (스킵 - 변경 없음)
# ✓ npm run build (성공)
# ✗ lint-error-fixer 검증 (TypeScript 타입 에러 발견!)
# ✗ npm run dev 실행 안 함 (중단)
#
# 오류 보고서:
# ─────────────────────────────────
# ❌ src/features/hero/ui/Hero.tsx:10
#    - Type 'string' is not assignable to type 'number'
#    - Fix: heroCopy.title을 숫자로 수정 필요
# ─────────────────────────────────
#
# 수정 후:
# /project-start (다시 실행)
```

### 예시 3️⃣: 빌드 실패
```bash
# 1. /project-start 실행 (빌드 실패)
/project-start

# 자동 감지:
# ✗ npm run build (실패)

# 자동 재시도:
# ✓ npm install (의존성 재확인)
# ✓ npm run build (재시도)

# 결과:
# ✗ 빌드 여전히 실패 → 오류 보고서 제시
# 또는
# ✓ 빌드 성공 → lint-error-fixer 검증 진행
```

### 예시 4️⃣: 보안 취약점 감지
```bash
# 1. 의존성 업데이트 후
# npm install

# 2. /project-start 실행
/project-start

# 자동 실행:
# ✓ npm install (변경 감지)
# ✓ npm run build (성공)
# ⚠️ lint-error-fixer 검증 (보안 취약점 발견!)
# ✗ npm run dev 실행 안 함 (중단)
#
# 오류 보고서:
# ─────────────────────────────────
# ⚠️ npm audit (CRITICAL)
#    - 패키지명: lodash@<4.17.21
#    - 문제: Prototype Pollution
#    - 수정: npm install lodash@latest
# ─────────────────────────────────
```

---

## 🔧 **configuration 파일**

### package.json 모니터링 항목
```json
{
  "dependencies": {
    // 추가/수정 감지
    "react": "^18.3.1",
    "vite": "6.3.5",
    "motion": "*"
  },
  "devDependencies": {
    // 추가/수정 감지
    "typescript": "^5.9.3",
    "vite": "6.3.5"
  }
}
```

### package-lock.json 비교
```
package.json의 의존성이 package-lock.json의 설치된 버전과 다르면
→ npm install 자동 실행
```

---

## 📈 **성능 개선**

### 실행 시간 비교

**기존 (무조건 install + build + dev):**
```
├─ npm install: 2-5초
├─ npm run build: 0.8초
└─ npm run dev: 0.1초
────────────────────
총 소요 시간: 3-6초 ⏱️
```

**개선 (스마트 조건부 실행):**
```
코드만 수정한 경우:
├─ npm install: 스킵 ✓
├─ npm run build: 0.8초
└─ npm run dev: 0.1초
────────────────────
총 소요 시간: 1초 ⚡ (3-6배 빠름!)

라이브러리 추가한 경우:
├─ npm install: 2-5초
├─ npm run build: 0.8초
└─ npm run dev: 0.1초
────────────────────
총 소요 시간: 3-6초 (필요할 때만 실행)
```

---

## ✨ **에이전트 자동 검증 (lint-error-fixer)**

빌드 성공 후 **lint-error-fixer 에이전트가 자동으로 검증:**

### 검증 항목
✅ TypeScript 타입 에러 확인
✅ Import 경로 유효성 검사
✅ 미사용 변수 및 함수 감지
✅ 의존성 상태 검사
✅ 보안 취약점 분석 (npm audit)
✅ 빌드 결과 평가
✅ 코드 구조 분석
✅ ESLint 규칙 준수 여부

### 검증 결과

**✅ 모든 검증 통과:**
- npm run dev 자동 실행
- 개발 서버 로컬에서 시작
- http://localhost:3000 접속 가능

**❌ 오류 발견:**
- npm run dev 실행 안 함 (중단)
- 상세 오류 보고서 제시
- 오류 수정 방법 제시
- 수정 후 다시 /project-start 실행

---

## 🎊 **결과**

명령어 한 번으로 **안전한 프로젝트 실행**을 얻을 수 있습니다!

```
/project-start
    ↓
[스마트 조건부 빌드]
    ↓
npm install (필요 시만) + npm run build
    ↓
[에이전트 자동 검증]
    ↓
✓ 오류 없음 → npm run dev (로컬 실행) ✓
✗ 오류 발견 → 오류 보고서 제시 (실행 중단)
    ↓
개발 서버 준비 완료
http://localhost:3000
```

---

## ⚙️ **에이전트 검증 설정**

lint-error-fixer 에이전트는 다음을 자동으로 확인합니다:

```typescript
// TypeScript 타입 검사
interface Project {
  title: string;
  description: string;
  tech: string[];
}
// ✅ 올바른 타입 사용
// ❌ 타입 오류 → 오류 보고

// Import 경로 검증
import { Button } from "@/shared/ui";
// ✅ 유효한 경로
// ❌ 잘못된 경로 → 오류 보고

// 미사용 변수 감지
const unusedVar = "test";  // ❌ 미사용 → 오류 보고
const usedVar = "test";    // ✅ 사용됨 → OK
```

---

## 📋 **요약: 검증된 안전한 개발 환경**

### 명령어 한 번으로 완전한 자동화 ✨

```
/project-start
    │
    ├─ 1️⃣ 스마트 빌드 (필요 시만)
    │   └─ package.json 변경 감지 → npm install
    │   └─ 코드 수정 감지 → npm run build
    │
    ├─ 2️⃣ lint-error-fixer 검증
    │   ├─ TypeScript 타입 체크
    │   ├─ Import 경로 검증
    │   ├─ 보안 취약점 분석
    │   ├─ 코드 품질 검사
    │   └─ ESLint 규칙 확인
    │
    └─ 3️⃣ 결과
        ├─ ✓ 검증 통과 → npm run dev (로컬 실행)
        └─ ✗ 오류 발견 → 상세 보고서 (실행 중단)
```

### 얻을 수 있는 이점

| 항목 | 이전 | 현재 |
|------|------|------|
| **실행 방식** | 수동으로 명령어 입력 | `/project-start` 한 번 |
| **오류 검증** | 실행 후 발견 | 실행 전 미리 감지 |
| **속도** | 느림 | 최적화됨 (필요 시만) |
| **안전성** | 낮음 | 높음 (검증됨) |
| **디버깅** | 어려움 | 상세 보고서 제시 |

### 개발 워크플로우

```
코드 수정
    ↓
/project-start (한 번의 명령)
    ├─ 자동 빌드
    ├─ 자동 검증
    └─ 자동 실행
    ↓
✓ 오류 없음 → 개발 시작
✗ 오류 발견 → 보고서 보고 수정 → 다시 /project-start
```

---

## 🎯 **핵심 기능**

### ✅ 스마트 조건부 실행
- package.json 변경 자동 감지
- 필요한 작업만 수행
- 개발 속도 향상

### ✅ 에이전트 기반 검증
- lint-error-fixer가 자동으로 검증
- 오류 발견 시 로컬 실행 중단
- 안전한 개발 환경 보장

### ✅ 상세한 오류 보고
- 어느 파일의 어느 줄에서 오류 발생했는지 명확
- 오류 원인 설명
- 수정 방법 제시

### ✅ 자동 재시도
- 빌드 실패 시 자동 재시도
- 의존성 문제 자동 해결
- 최대한 많은 상황을 자동으로 처리

---

**이제 /project-start 명령어로 완전히 자동화되고 검증된 개발 환경을 사용할 수 있습니다!** 🚀
